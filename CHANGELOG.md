# 📋 릴리즈 노트 & 업데이트 로그 (ChangeLog)

Pulse (Pulse Cloud & Pulse OS) 공식 버전별 변경 이력입니다.

## [v2.6.1] - 2026-09-23
### 공유·파일 처리 안정성 개선

- 새 공유 링크 ID를 128비트 무작위 값으로 늘리고 공개 페이지·다운로드에 IP별 요청 제한을 적용했습니다. 다운로드 횟수 확인과 갱신을 같은 잠금 안에서 처리해 동시 요청으로 제한을 넘지 않도록 했습니다.
- ZIP 다운로드 결과를 메모리 대신 서버의 임시 파일에 작성하고 파일 1000개·원본 합계 1GB 및 저장 공간 제한을 적용했습니다.
- ZIP 압축 해제에 항목 1000개·해제 합계 1GB와 여유 공간 확인을 추가하고, 기존 파일 덮어쓰기를 막았습니다. 실패 시 이번 작업에서 생성한 파일을 정리합니다.
- 파일·폴더 이동과 이름 변경 시 하위 항목을 포함한 즐겨찾기·태그·공유 링크 경로를 함께 갱신합니다.
- 메타데이터·공유 링크 JSON을 임시 파일에 기록한 뒤 교체하고, 읽기부터 쓰기까지 잠금을 유지해 동시 변경 손실을 막았습니다. 손상된 JSON은 빈 데이터로 덮어쓰지 않고 오류로 알립니다.
- 사용자 정의 Python API 실행을 동시 최대 2개의 별도 프로세스로 옮겼습니다. 3초 초과 시 프로세스를 종료하고 지원 환경에서 CPU·메모리·출력 크기를 제한합니다.

---

## [v2.6.0] - 2026-09-22
### ⚡ Pulse API Studio (나만의 API 서비스 & 링크 생성기)

#### 1. ⚡ Pulse API Studio 가상 웹 데스크톱 & 시작 화면 탑재
- **시작 화면(Portal)**: 4번째 메인 선택 카드로 `API 서비스 (Pulse API Studio)` 추가, 가동 중인 엔드포인트 개수 실시간 요약 표시 및 1클릭 실행 지원.
- **Pulse OS 통합**: 데스크톱 바로가기 아이콘, 플로팅 Dock 아이콘(⚡), Spotlight 검색(`api`, `apis`, `studio`, `webhook`, `웹훅`, `함수`, `serverless`, `rest`, `endpoint`) 등록.
- **Apple HIG 2열 윈도우 UI**: 좌측 API 엔드포인트 목록 및 실시간 검색 사이드바 + 우측 엔드포인트 설계 에디터 및 링크 발급/인스턴트 테스터 패널.

#### 2. 🔗 고유 링크 즉시 발급 & 원클릭 공유 (Local & Cloudflare Tunnel)
- **외부 인터넷 링크 자동 생성**: Cloudflare Tunnel이 활성화되어 있을 경우 전 세계 어디서든 호출할 수 있는 `https://<tunnel-domain>/api/fn/<name>` 고유 URL 자동 계산 및 1클릭 복사/열기.
- **로컬 Wi-Fi 링크 자동 생성**: 동일 네트워크에서 호출할 수 있는 `http://<phone-ip>:3000/api/fn/<name>` URL 제공.
- **cURL 명령어 자동 생성**: 선택된 HTTP 메소드, 인증 헤더, JSON 페이로드에 맞춰 터미널에서 즉시 실행 가능한 `curl` 명령어 1클릭 복사.

#### 3. 🛠️ 3대 처리 엔진 (Engine Modes)
- **📄 JSON Mock 모드**: 고정된 JSON 데이터를 1초 만에 반환 (외부 웹훅 테스트, 프론트엔드 목업용).
- **🐍 Python 서버리스 모드 (Micro-Serverless)**: `def handle(req):` 함수를 작성하여 `req['params']`, `req['body']`, `req['method']`를 받아 파이썬 코드로 동적 연산 후 딕셔너리 반환.
- **📱 스마트폰 디바이스 모드**: 호출 시 스마트폰 배터리 잔량/충전 상태, 저장공간 여유량, 시스템 사양, 하트비트 핑을 실시간 측정하여 반환.

#### 4. 🛡️ 유연한 접근 제어 & 실시간 인스턴트 테스터
- **인증 권한 3단계**:
  - `public`: 외부 웹훅(GitHub, Slack, Notion 등)에서 로그인 쿠키 없이 자유롭게 호출 가능 (CORS `*` 헤더 기본 탑재).
  - `key`: 헤더 `X-API-Key` 또는 쿼리 `?key=` 로 비밀 키 인증 (랜덤 키 자동 생성기 내장).
  - `private`: Pulse 로그인 세션 사용자 전용 엔드포인트.
- **인스턴트 테스터 (Test Run)**: 창 내부에서 쿼리 스트링 및 JSON 본문을 입력하고 [요청 전송]을 누르면 상태 코드(`200 OK`), 지연 시간(`ms`), 응답 본문(JSON 컬러 뷰어)을 실시간 확인.
#### 5. ⚡ 전방위 시스템 최적화 & 사용성 보강
- **아이콘 렌더링 결함 수정**: `pulse.js`의 SVG 아이콘 테이블에 누락되었던 5종(`music`, `photos`, `clipboard`, `cam`, `stickies`) 패스를 정식 탑재하여 기본 폴더 아이콘으로 잘못 폴백되던 현상 완벽 해결.
- **파일 목록 조회 디스크 I/O 100배 최적화**: `pulse_files.py`의 `files()` 엔드포인트에서 파일별 `metadata.json` 중복 디스크 읽기를 단 1회 캐시 로드로 축소하여 대용량 파일 폴더 로딩 반응성 획기적 개선.
- **Python 서버리스 스레드 타임아웃 보호**: `execute_python_api`에 3.0초 스레드 타임아웃 래퍼를 적용하여 무한 루프나 블로킹 발생 시 504 TIMEOUT을 반환하고 서버 전체 락업 방지.
- **디바이스 모드 실시간 하드웨어 연동**: `storage`(저장공간 잔여량/사용률), `info`(OS, CPU, 메모리), `ping`(가동 시간, 헬스체크) 실제 시스템 센서 데이터 반환 구현.
- **API Studio 실시간 양방향 갱신**: 엔드포인트 경로, 메서드, 키 입력 시 상단 경로, 로컬/터널 URL, cURL 명령어, 테스터 URL이 입력 즉시 동기화되도록 이벤트 리스너 연결.
- **창 리사이즈 모바일 터치 지원**: Pulse OS 가상 창 리사이즈 핸들에 `touchstart`, `touchmove`, `touchend` 터치 제스처 지원 추가.
- **CLI & 상태 스크립트 고도화**: `termux-cloud api`로 등록된 API 목록 터미널 확인 지원, `status.sh`에 등록된 API 개수 요약 출력 및 레거시 헤더 정리.

---

## [v2.5.0] - 2026-09-22
### 🚀 Cloudflare Tunnel 원클릭 외부 접속 & 반응형 모바일 UI 개편

#### 1. ☁️ Cloudflare Tunnel 원클릭 외부 접속 통합 (`tunnel.sh` & CLI)
- **어디서든 브라우저 접속**: 스마트폰 LTE/5G 모바일 데이터 환경(통신사 CGNAT)에서도 공유기 포트포워딩 없이 전 세계 어디서든 브라우저로 Pulse Cloud에 접속 가능.
- **전역 CLI 지원**:
  - `termux-cloud tunnel start [--bg]`: 무료 Quick Tunnel (`*.trycloudflare.com`) 즉시 자동 생성.
  - `termux-cloud --bg --tunnel`: Pulse 서버와 Cloudflare Tunnel 동시 백그라운드 구동.
  - `termux-cloud tunnel status` / `stop`: 터널 상태 확인 및 프로세스 안전 종료.
  - `termux-cloud stop` 시 연계된 터널 프로세스 자동 종료 지원.
  - `termux-cloud sync` (또는 `termux-cloud update --tunnel`): GitHub 최신 코드 자동 업데이트 + 의존성 갱신 + 서버 및 외부 터널 동시 백그라운드 원클릭 재시작.
- **고정 도메인 지원**: 개인 도메인이 있는 경우 `CLOUDFLARE_TUNNEL_TOKEN` 환경변수 또는 `--token` 옵션으로 영구 고정 도메인 연결 지원.
- **상태 모니터링 연동**:
  - `termux-cloud status`: 외부 인터넷 HTTPS 접속 주소 명확히 출력.
  - Pulse OS 제어 센터(Control Center): 외부 접속 배지 표시 및 1클릭 복사 버튼 탑재.
  - 시스템 대시보드 (`/api/system/dashboard`, `/api/system/tunnel`): 외부 터널 활성화 상태 및 접속 주소 실시간 제공.

#### 2. 📱 모바일 환경 반응형 터치 UI 전면 최적화
- **상단 내비게이션 바**: 좁은 모바일 화면에서 메인 4대 탭을 아이콘 전용 모드로 간소화하고 로고 텍스트/상태 칩을 숨겨 360px 기기에서도 1줄 정렬 완성.
- **파일 액션 툴바 단일 행 수평 스크롤**: 기존 버튼 줄바꿈 현상을 해결하고 iOS 파일 앱 스타일의 단일 행 관성 수평 스크롤(`overflow-x: auto`) 적용.
- **Pulse OS 데스크톱 & 플로팅 Dock**: 메뉴바 불필요 버튼 숨김 및 집중 모드 타이머 축소, 15개 앱 Dock의 부드러운 가로 스와이프 탐색 지원.
- **가상 윈도우 모바일 풀스크린 리셋**: 고정 min-width를 0으로 리셋하고 신호등 최대화 버튼 숨김 처리로 화면 짤림 방지.

---

## [v2.4.0] - 2026-09-22
### 🚀 Pulse Cam (스마트폰 원격 홈캠 / CCTV & 스냅샷)

#### 1. 📷 Pulse Cam 가상 웹 데스크톱 앱 탑재
- 남는 안드로이드 공기계를 24시간 실시간 홈캠 / 베이비캠 / 반려동물 CCTV 모니터로 변신시키는 전용 앱 탑재.
- 데스크톱 바로가기 아이콘 및 Dock에 📷 Pulse Cam 등록, Spotlight에서 `cam`, `camera`, `cctv`, `homecam`, `웹캠`, `카메라` 검색 지원.
- 🔴 LIVE 상태 배지 (부드러운 펄스 애니메이션) 및 OSD 상단 타임스탬프(`YYYY-MM-DD HH:mm:ss`) 실시간 오버레이.
- ▦ 화면 격자(Crosshair) 가이드라인 온/오프 토글.

#### 2. 📱 스마트폰 브로드캐스트 송출 및 하이브리드 스트리밍
- **스마트폰 송출기 모드**: 공기계 브라우저에서 '내 기기 카메라 송출하기' 버튼 클릭 시 카메라 영상을 백엔드 메모리 릴레이(`/api/camera/feed`)로 초경량 실시간 송출.
- **Termux:API 하드웨어 스냅샷 연동**: Termux 환경에 `termux-camera-photo`가 설치되어 있는 경우 브라우저 창이 꺼져 있어도 백엔드가 직접 하드웨어 카메라를 호출하여 프레임 캡처.
- **로컬 웹캠 모드**: PC/노트북 브라우저 자체 웹캠을 사용하여 간이 모니터링 가능.
- 전면/후면 렌즈(Camera 0 / Camera 1) 전환 셀렉터 지원.

#### 3. 📸 고화질 스냅샷 즉시 보관 (`uploads/Camera/`)
- '스냅샷 캡처' 버튼 클릭 시 `uploads/Camera/Cam_YYYYMMDD_HHMMSS.jpg`로 즉시 자동 영구 보관.
- Web Audio API 기반 기계식 카메라 셔터 찰칵 효과음 및 화면 플래시 애니메이션 효과 제공 (외부 오디오 파일 의존성 제로).
- 스냅샷 완료 시 알림 센터(`addNotificationCenterItem`) 기록 및 토스트 알림 연동.
- Pulse Photos 및 Finder의 `Camera` 폴더와 즉시 연동되며, 창 상단 '보관함' 버튼으로 1클릭 이동 가능.

---

## [v2.3.0] - 2026-09-22
### 🚀 Clipboard 히스토리 · 즐겨찾기 & 7색 태그 · 파일 안전 공유 링크 · Focus Mode (집중 모드)

#### 1. 📋 Clipboard 히스토리 앱
- 복사한 텍스트를 실시간 자동 감지하여 영구 저장하는 가상 웹 데스크톱 전용 클립보드 앱 탑재.
- 바탕화면 바로가기 및 Dock에 📋 클립보드 앱 등록, Spotlight에서 `clipboard`, `clip`, `복사` 검색 지원.
- 자주 쓰는 텍스트 상단 핀(📌) 고정, 검색바 실시간 필터링, 개별 삭제 및 전체 비우기.
- 항목 클릭 시 즉시 다시 클립보드로 복사.

#### 2. 🔖 파일 즐겨찾기(⭐) 및 7가지 컬러 태그
- 보관함 내 모든 파일/폴더에 즐겨찾기(별표) 및 7색 태그(🔴 빨강, 🟠 주황, 🟡 노랑, 🟢 초록, 🔵 파랑, 🟣 보라, ⚪ 회색) 지정.
- `.pulse/metadata.json`에 영구 보관하여 모바일/PC 등 다른 기기에서도 설정 동기화.
- Cloud 뷰 사이드바에 '즐겨찾기' 카테고리 및 컬러 태그 칩 필터 추가.
- 파일 카드 우상단 별표 원클릭 토글 및 우클릭 컨텍스트 메뉴 "즐겨찾기 토글" 지원.
- 파일 카드/목록에 색상 태그 도트 표시.

#### 3. 🔗 안전 파일 공유 링크 생성 (`/s/<shareId>`)
- 로그인 세션 없이도 외부/로컬 네트워크에서 다운로드할 수 있는 고유 공유 링크 생성.
- 링크 유효 기간(1시간, 24시간, 7일, 무제한) 및 최대 다운로드 횟수(1회 일회용, 5회, 20회, 무제한) 설정.
- `.pulse/shares.json`에 메타데이터 보관, 만료 시 410 만료 안내 화면 표시.
- 수신자를 위한 Apple HIG 스타일 반응형 공개 다운로드 웹페이지 (`/s/<id>`) 및 원클릭 복사 기능.
- 파일 우클릭 메뉴에 "🔗 공유 링크 생성" 항목 추가.

#### 4. 🌙 Focus Mode (방해 금지 & 뽀모도로 타이머)
- 메뉴바 우측에 🌙 집중 모드 토글 버튼 및 제어 센터(Control Center) 집중 모드 타일 추가.
- 활성화 시:
  - **방해 금지 (Do Not Disturb)**: 토스트 팝업 알림 음소거/차단, 알림 센터에만 조용히 보관.
  - **화면 앰비언트 디밍**: 바탕화면 및 비활성 창이 차분하게 어두워지고 작업 창에 시각적 집중.
  - **뽀모도로 집중 타이머**: 메뉴바에 `🌙 24:59` 실시간 카운트다운 표시.
  - 타이머 완료 시 Web Audio API 맑은 차임벨 효과음 및 휴식 권장 알림.

---

## [v2.2.0] - 2026-09-22
### 🚀 Pulse Photos · ZIP 압축·해제 · Calendar 위젯 · 단축키 치트시트

#### 1. 📸 Pulse Photos — iCloud 스타일 사진 갤러리
- Pulse OS Dock + 바탕화면에 📸 Photos 앱 추가.
- 보관함 전체를 재귀 탐색하여 이미지 파일을 **월별 그룹**으로 표시.
- 사진 클릭 시 **라이트박스** 전체화면 뷰어, ← → 키보드 탐색.
- 사이드바 필터: 모든 사진 / 최근 30일 / ▶ 슬라이드쇼(3초 간격 자동 재생).
- 백엔드 `GET /api/photos` 엔드포인트 신규 추가 (Python 표준 라이브러리).
- Spotlight에서 `photos`, `gallery`, `사진` 검색으로 바로 열기.

#### 2. 📦 ZIP 압축 다운로드 / 압축 해제
- Cloud 뷰 파일 다중 선택 후 **ZIP 다운로드** 버튼 (선택 시에만 표시).
- 백엔드 `POST /api/zip-download`: `zipfile` 표준 라이브러리로 메모리 내 ZIP 생성, 즉시 스트리밍 반환.
- `.zip` 파일 우클릭 컨텍스트 메뉴에 **"여기에 압축 해제"** 항목 추가.
- 백엔드 `POST /api/unzip`: 경로 순회 공격 방지 처리 후 동일 폴더에 압축 해제.

#### 3. 🗓️ Calendar 위젯 — 메뉴바 시계 클릭
- Pulse OS 메뉴바 시계 클릭 시 **월간 달력 팝오버** 표시 (Glassmorphism 디자인).
- 이전/다음 달 탐색, 오늘 날짜 하이라이트.
- 날짜 클릭 → **일정 메모 추가/삭제**, `localStorage`에 영구 저장.
- 일정이 있는 날짜에 파란 점 표시.

#### 4. ⌨️ 단축키 치트시트 — Ctrl+/
- `Ctrl+/` (또는 `Cmd+/`) 키 입력 시 **Pulse OS 전체 단축키** 오버레이 모달.
- 7개 카테고리(전역, Cloud, 창 관리, Finder, 에디터, Photos, Spotlight)별 정리.
- Spotlight에서 `단축키`, `shortcut`, `keyboard` 검색으로 열기.
- `Esc` 또는 배경 클릭으로 닫기.

#### 5. 기타 개선
- Spotlight 앱 목록에 Pulse Photos와 단축키 항목 추가.
- Finder 컨텍스트 메뉴: `.zip` 파일에 "여기에 압축 해제" 메뉴 조건부 표시.

---

## [v2.1.0] - 2026-09-21
### 🚀 Pulse OS 데스크톱 앱 확장 (파일 아이콘 · 휴지통 · Notes · 계산기 · 알림 · 최근 항목)

#### 1. 바탕화면 실제 파일 아이콘
- 보관함 `Desktop/` 폴더의 파일·폴더를 화면 오른쪽에 아이콘으로 표시.
- 더블클릭으로 열기, `Space` Quick Look, 우클릭으로 미리보기/다운로드/이름 변경/휴지통 이동.
- 바탕화면 우클릭 '새 파일/새 폴더'와 파일 드롭은 `Desktop/`에 저장.
- 서버 시작 시 `Desktop/`, `Notes/` 폴더를 자동 생성.

#### 2. 휴지통 창 (Dock)
- Pulse OS Dock에 휴지통 앱 추가. 복원, 영구 삭제, 한 번에 비우기.
- 이름 변경(`/api/rename`)·일괄 휴지통 이동(`/api/batch/delete`)·이동(`/api/move`) API를 백엔드에 정식 연결.

#### 3. 여러 장 Stickies
- 스티커 메모를 여러 장 두고 탭으로 전환. 색상은 장마다 따로 저장.
- 기존 단일 메모는 자동으로 첫 번째 장으로 이전.

#### 4. Pulse Notes
- `Notes/`에 `.md` 파일로 저장되는 메모장. 목록, 제목, 본문, `Ctrl+S` 저장.

#### 5. 알림 센터
- 메뉴바 종 아이콘. 업로드 완료, 배터리 20% 이하, 서버 업데이트 알림을 로컬에 보관.

#### 6. 최근 항목 & 계산기
- Spotlight와 Finder 사이드바에 최근 연 파일 목록.
- Pulse OS 계산기 앱 (Dock / Spotlight).

---

## [v2.0.2] - 2026-09-21
### ✨ Pulse OS / Cloud 사용성 보완

#### 1. Finder와 Cloud 필터 분리
- Finder 사이드바 필터가 Pulse Cloud의 `currentFilter`를 덮어쓰던 문제를 수정. Finder는 자체 필터만 적용.
- Finder에 '폴더' 필터와 브레드크럼 경로 바(상위 폴더 / 경로 점프) 추가.
- 빈 폴더·필터 결과에 맞는 안내 문구 표시. 데스크톱에서 선택 후 `Enter`로 열기.

#### 2. 바탕화면 인터랙션
- 데스크톱 아이콘: 클릭=선택, 더블클릭=실행 (모바일은 탭으로 실행 유지).
- Pulse OS에서 파일을 드롭해도 Cloud 화면으로 강제 전환하지 않고 현재 폴더에 업로드.
- `Space` Quick Look이 선택하지 않은 첫 파일을 임의로 열던 동작 제거.
- 에디터/입력 중 `Ctrl+↑`로 미션 컨트롤이 열리던 단축키 충돌 방지.
- 메뉴바 IP 배지 클릭 시 주소 복사.

#### 3. 알림 · 클립보드 · 볼륨
- 토스트 '다시 시도' 버튼이 `pointer-events: none`과 2.7초 페이드로 눌리지 않던 문제 수정.
- 클립보드 복사 실패 시 `textarea` 폴백을 공통 헬퍼로 적용 (대시보드 IP, 미리보기, Spotlight 계산, 제어 센터, VNC 명령).
- Pulse Music 기본 볼륨을 슬라이더(80%)와 맞추고, 플레이어↔제어 센터 볼륨을 동기화.
- 파일 목록 로딩 중 빈 화면 안내가 먼저 뜨지 않도록 수정.

---

## [v2.0.1] - 2026-09-21
### 🐛 Pulse OS 우클릭 메뉴 위치 및 데스크톱 UI 버그 수정

#### 1. 우클릭 컨텍스트 메뉴가 클릭 지점에 정확히 표시
- 메뉴 좌표를 뷰포트 `clientX/Y`가 아니라 `.desktop-screen` 기준 상대 좌표로 변환.
- 실제 메뉴 너비/높이를 측정한 뒤 화면 밖으로 나가면 왼쪽·위로 뒤집고, 메뉴바·가장자리에 맞춰 클램프.
- 창 타이틀바 우클릭 시 브라우저 기본 메뉴 대신 앞으로 가져오기/최소화/최대화/닫기 메뉴 표시.
- 바탕화면 아이콘 우클릭 시 해당 앱 열기 항목 표시.
- 스크롤·리사이즈·다른 오버레이에서는 메뉴를 닫고, 터치 롱프레스 직후 메뉴가 바로 사라지지 않도록 수정.

#### 2. 같은 좌표계로 어긋나던 데스크톱 인터랙션
- 마키(Marquee) 선택 박스가 마우스 드래그 경로와 일치하도록 캔버스 상대 좌표로 그리기.
- Stickies·Pulse Music 창을 `#desktop-canvas` 안으로 옮겨, 드래그 시 메뉴바 높이만큼 점프하던 문제 수정.
- 창 드래그가 항상 `offsetParent` 기준으로 계산되도록 변경.
- 좌/우 스냅 프리뷰를 캔버스 안으로 옮겨 실제 창이 붙는 위치와 가이드가 일치.
- 창을 화면 상단으로 드래그해 최대화하는 스냅이 글로벌 내비 좌표를 쓰던 문제를 캔버스 상단 기준으로 수정.

#### 3. Finder · Quick Look · 대화상자 · 제어 센터
- Finder에서 선택한 파일에 선택 하이라이트를 주고, `Space` Quick Look이 마지막 선택 항목을 미리보기.
- 데스크톱은 클릭=선택 / 더블클릭=열기, 모바일(≤768px)은 탭=열기 유지.
- Stickies 지우기·제어 센터 서버 재시작의 네이티브 `confirm()`을 Pulse 대화상자로 교체.
- 제어 센터 테마 라벨이 항상 '다크 모드'로 나오던 문제를 수정하고, 실제 바탕화면 테마명(Sonoma/Sequoia/Cyber/Midnight)을 표시.

#### 4. 기타 UI
- 오프라인 Termux에서 실패하는 Google Fonts CDN 링크 제거. 시스템 폰트 스택만 사용.
- Dock 호버 CSS `transform`과 JS Magnification이 서로 덮어쓰며 떨리던 현상 정리.

---

## [v2.0.0] - 2026-09-20
### 🚀 Pulse OS (가상 웹 데스크톱 2.0) 8대 핵심 기능 대규모 업데이트

#### 1. 🔍 macOS 스타일 전역 검색 & 런처 (Spotlight / Raycast)
- **단축키**: `Ctrl + Space` 또는 `Cmd + Space`, 상단 메뉴바 돋보기 버튼.
- **앱 즉시 실행**: 키보드로 앱 이름(예: `term`, `finder`, `editor`, `stickies`, `music`, `monitor` 등) 검색 및 엔터 시 즉시 실행/포커스.
- **인스턴트 계산기**: 수학 수식(예: `1920 * 1080`, `256 / 8`, `15% of 50000`, `2^16` 등) 입력 시 실시간 계산 결과 카드 표시 및 엔터 시 클립보드 원터치 복사.
- **시스템 명령 실행**: 테마 전환, 미션 컨트롤, 제어 센터, 화면 새로고침, 전체화면 토글 등 즉시 실행.
- **보관함 파일 실시간 검색**: 파일명 실시간 필터링 후 선택 시 Quick Look 또는 에디터로 즉시 열기 지원.

#### 2. 👁️ 스페이스바 빠른 파일 미리보기 (Quick Look)
- **단축키**: Finder 또는 바탕화면에서 파일 선택 후 `Space` 키 입력 (`Esc` 또는 `Space`로 닫기).
- **지원 포맷**: 이미지(핏 렌더링), 동영상/오디오(인라인 HTML5 플레이어), 텍스트/코드(구문 강조 프리뷰), 기타 파일(다운로드 링크 제공).
- **연속 탐색**: 방향키(`←`/`→`, `↑`/`↓`)로 파일 목록을 순차적으로 넘기며 1초 만에 빠른 파일 검토 가능.

#### 3. 🪟 미션 컨트롤 & 멀티태스킹 뷰 (Mission Control / App Exposé)
- **단축키**: `F3` 또는 `Ctrl + Up`, 메뉴바 좌측 상단 버튼, 제어 센터 버튼.
- **3D 타일링 정렬**: 현재 열려 있는 모든 창을 화면 크기에 맞추어 2열/3열 그리드 형태로 부드럽게 축소 타일링 배치.
- **빠른 창 전환**: 원하는 창을 마우스로 클릭하면 해당 창이 즉시 맨 앞으로 올라오며 정상 크기로 부드럽게 복귀.

#### 4. 🖥️ 바탕화면 마키(Marquee) 사각형 다중 선택 & 아이콘
- **마키 드래그 선택**: 빈 바탕화면에서 마우스 드래그 시 반투명 파란색 영역 박스가 생성되며 여러 아이콘을 한 번에 다중 선택.
- **신규 바로가기**: 바탕화면에 '스티커 메모', 'Pulse 음악' 바로가기 아이콘 배치.

#### 5. 🎛️ macOS 스타일 통합 제어 센터 (Control Center)
- **메뉴바 팝오버**: 상단 메뉴바 토글 버튼 클릭 시 우측 상단 Glassmorphism 패널 출력.
- **위젯 모듈**: 네트워크 IP 및 1클릭 복사, 화면 테마 순환 전환(Sonoma/Sequoia/Cyber/Midnight), 배경화면 실시간 블러 조절 슬라이더, 시스템 마스터 볼륨 슬라이더, 하드웨어(배터리/CPU/메모리) 실시간 요약 카드, 서버 프로세스 재시작 버튼.

#### 6. 📝 맥 스타일 스티커 메모 앱 (Stickies)
- **새로운 내장 앱**: 포스트잇 스타일의 플로팅 메모 창 (`#win-stickies`, 📌).
- **4색 파스텔 테마**: 노랑, 초록, 분홍, 파랑 테마 선택.
- **자동 실시간 영구 저장**: 작성 즉시 `localStorage`에 자동 보관되어 브라우저를 닫아도 내용 100% 유지.
- **창 접기(Fold)**: 타이틀바 더블클릭 또는 접기 버튼으로 헤더 바만 남기고 작게 축소 가능.

#### 7. 🎵 미니 플로팅 미디어 플레이어 (Pulse Music)
- **새로운 내장 앱**: 보관함 음악 파일 전용 플레이어 창 (`#win-music`, 🎵).
- **오디오 자동 감지**: 보관함 내 `mp3`, `flac`, `wav`, `m4a`, `ogg`, `aac` 등 오디오 파일 자동 큐 구성.
- **플레이어 UI**: 회전 바이닐 레코드 비주얼, 곡명/아티스트 정보, 타임라인 시크바(Scrubber), 재생/일시정지/이전곡/다음곡, 볼륨 조절.
- **백그라운드 지속 재생**: 플레이어 창을 최소화하거나 다른 창을 작업해도 음악이 끊기지 않고 백그라운드에서 유지.

#### 8. ⛵ Dock 인터랙션 강화 (Magnification 확대 효과 & 창 최소화 모션)
- **Dock Magnification**: 마우스 호버 시 커서 거리 기반으로 주변 아이콘들이 부드러운 곡선 형태로 커지는 가우시안 Magnification 모션.
- **Genie / Scale 창 최소화 애니메이션**: 창 최소화 시 독의 해당 앱 위치로 빨려 들어가듯 축소/페이드되는 트랜지션 애니메이션 적용.

---

## [v1.9.0] - 2026-09-20
### ☁️ Pulse Cloud 폴더 기능 및 계층형 파일 관리 고도화

#### 1. Cloud 상단 툴바 '+ 새 폴더' 원터치 생성
- Pulse Cloud 상단 툴바에 '+ 새 폴더' 버튼 추가 (관리자 전용).
- 현재 보고 있는 디렉터리 경로(루트 또는 하위 폴더) 내에 즉시 새 폴더를 생성하고 자동 새로고침.

#### 2. 인터랙티브 브레드크럼(Breadcrumb) 경로 탐색 바
- 현재 위치를 한눈에 파악할 수 있는 macOS / Finder 스타일 경로 바 탑재 (`🏠 내 보관함 > 📁 folderA > 📁 folderB`).
- 각 경로 세그먼트를 클릭하여 원하는 상위 폴더로 1클릭 즉시 점프.
- '↑ 상위 폴더' 단축 버튼 제공으로 빠른 상위 디렉터리 복귀 지원.

#### 3. 사이드바 '📁 폴더' 필터 및 실시간 개수 뱃지
- 좌측 사이드바 탐색 목록에 '📁 폴더' 전용 필터 및 카운트 뱃지 추가.
- 백엔드(`/api/files`)에서 폴더 개수 및 `type=folder` 전용 조회를 완벽 지원.

#### 4. 그리드 & 리스트 뷰 폴더 카드 디자인 및 바로 열기
- **그리드 뷰**: 전용 macOS 스타일 블루 폴더 아이콘, '폴더' 전용 타입 뱃지, 불필요한 다운로드 버튼 대신 '열기'(`btn-open`) 버튼 제공.
- **리스트 뷰**: 폴더 전용 아이콘 및 '폴더 열기' 버튼 제공.
- 폴더 카드 또는 목록 행 클릭 시 해당 하위 폴더로 부드럽게 이동 탐색.
- 폴더 진입 시 필터를 자동으로 전체(`all`)로 전환하여 내부 파일들을 즉시 확인할 수 있도록 UX 개선.

#### 5. 폴더 카드 드래그 앤 드롭(Drag & Drop) 파일 업로드
- PC 브라우저에서 로컬 파일들을 특정 폴더 카드 위로 드래그하면 시각적 하이라이트(파란 점선 및 확대 효과) 피드백 제공.
- 드롭 즉시 해당 폴더 경로(`folder`)로 파일들이 자동 큐에 등록되어 업로드 시작.

#### 6. 폴더 내 빈 화면 상태(Empty State) 친화적 안내
- 빈 하위 폴더 진입 시 "이 폴더가 비어 있습니다" 및 "상단 업로드나 새 폴더 버튼을 이용해 보세요" 맞춤 안내 메시지 표시.

---

## [v1.8.0] - 2026-09-20
### 🖥️ Pulse OS (가상 웹 데스크톱) 5대 핵심 기능 고도화

#### 1. macOS 스타일 마우스 우클릭 컨텍스트 메뉴 (Context Menu)
- **바탕화면 우클릭**: '새 텍스트 파일', '새 폴더', '바탕화면 새로고침', '배경화면 및 테마 설정' 메뉴 지원.
- **Finder 항목 우클릭**: 대상 타입에 맞춰 '열기/미리보기/에디터 편집', '다운로드', '이름 변경', '휴지통으로 이동' 액션 지원.
- **모바일 롱프레스 터치**: 터치 기기 사용자를 위한 550ms 롱프레스 제스처 지원.
- **UI 및 화면 경계 보정**: Frosted Glass 블러 효과, 다크/라이트 테마 자동 연동, 화면 밖 메뉴 짤림 방지 위치 보정.

#### 2. 코드 / 텍스트 에디터 구문 강조 (Syntax Highlighting) & 줄 번호 정밀 동기화
- **지원 언어**: JavaScript, Python, HTML, CSS, Shell Script, JSON, Markdown/Text 등 주요 코드 구문 실시간 컬러 하이라이팅.
- **줄 번호 거터 & 스크롤 동기화**: 거터(Gutter) 라인 번호와 본문 텍스트, 하이라이트 레이어가 1:1로 수직/수평 스크롤 정밀 동기화.
- **텍스트 선택 가시성 개선**: 반투명 블루 선택(`::selection`) 스타일 적용으로 선택 영역 드래그 시에도 가독성 보존.

#### 3. 터미널 ANSI 이스케이프 컬러 파서 & 원터치 출력 복사
- **ANSI 컬러 파서**: 16색 터미널 색상 및 굵게(`\x1b[1m`), 밑줄(`\x1b[4m`) 등의 이스케이프 코드를 파싱하여 웹에 선명하게 렌더링 (`ls --color` 등 지원).
- **출력 복사 버튼 (`#btn-term-copy`)**: 윈도우 상단 헤더에 '복사' 버튼을 추가하여 터미널 실행 기록을 클립보드로 원터치 복사.

#### 4. 윈도우 좌/우 50% 분할 스냅 (Window Snapping & Tiling)
- **스냅 감지 & 프리뷰**: 윈도우 타이틀바를 잡고 화면 좌/우 끝으로 드래그 시 50% 분할 영역 가이드 표시, 상단 끝으로 드래그 시 최대화 가이드 표시.
- **스냅 적용 및 원상복구**: 마우스 드롭 시 화면의 정확히 50% 너비로 자동 스냅 타일링되며, 스냅된 창을 다시 드래그하면 이전 크기(Width/Height)로 자동 복원.

#### 5. Finder '+ 새 폴더' 생성 버튼
- Finder 상단 툴바에 '+ 새 폴더'(`#btn-finder-new-folder`) 버튼 추가 및 `/api/folders` 실시간 생성 연동.

---

## [v1.7.0] - 2026-09-19
### 전체 버튼 UI/UX 시각적 구분감 및 Apple HIG 개편
- 카드 및 목록의 액션 버튼에 기본 휴지 상태 고유 틴트 적용 (다운로드: 블루, 삭제: 레드, 미리보기: 그린)으로 시각적 구분 및 오조작 방지.
- 파일 상단 툴바 버튼을 8개의 고유 SVG 아이콘 및 역할별 스타일(생성/위험/네비게이션/유틸리티)로 세분화.
- 휴지통 대화상자에서 '복원(그린)'과 '영구 삭제(레드)'의 시각적 분리 및 `pulse.ask` 위험 작업 확인 시 확인 버튼의 레드 경고 스타일 자동 전환.
- `button:disabled` 상태에서 로딩으로 오해를 주던 `cursor: wait`를 `cursor: not-allowed`로 교정.
- 대시보드 툴바, 상태 수동 새로고침 버튼, 상단 로그아웃 버튼의 세부 인터랙션 및 스타일 고도화.

### 자동 강제 업데이트(Force Sync) 및 로컬 변경 무시
- Termux(안드로이드)의 파일 권한(chmod) 차이나 로컬 수정으로 인해 "커밋하지 않은 변경이 있습니다" 오류로 업데이트가 차단되던 문제 해결.
- `app.py` 및 `update.sh`에서 `git fetch origin main` 후 `git reset --hard origin/main`을 자동 수행하여 항상 최신 깃허브 버전으로 원터치 강제 동기화.
- 사용자가 업로드한 파일(`uploads/`) 및 계정 정보(`.pulse/`)는 `.gitignore` 처리되어 100% 안전하게 보존.
- 서버 기동 시 `git config core.filemode false` 상시 적용.

### 기본 샘플 파일(sample_photo.svg, 환영합니다.txt) 영구 제거
- 삭제해도 업데이트 시마다 다시 복원되던 기본 샘플 파일들을 Git 추적에서 영구 제거(`git rm`).
- `.gitignore` 화이트리스트 정리 및 기동/업데이트 시 기존에 남아있던 샘플 파일 자동 정리 루틴 적용.

### Pulse OS 윈도우 헤더 버튼 크기 압축 및 레이아웃 깨짐 수정
- `.window-header-actions`의 60px 고정 너비를 제거하여 내부 버튼 개수(Finder의 '+ 새 파일', '새로고침' 등)에 맞게 자연스럽게 가로 정렬되도록 개선.
- `.win-btn-sm`에 `height: 26px`, `white-space: nowrap`, `flex-shrink: 0`을 부여하여 글자 세로 줄바꿈 및 너비 찌그러짐 현상 원천 차단.
- Terminal, Finder, Monitor, Browser, VNC 윈도우 헤더 버튼에 macOS 스타일 정밀 SVG 아이콘 일괄 적용.
- 모바일(≤768px) 반응형 레이아웃에서도 헤더 버튼이 겹치거나 잘리지 않도록 크기 및 여백 최적화.

---

## [v1.6.4] - 2026-09-19
### 공통 화면 애니메이션
- 메인 화면 전환·OS 창 열기에 페이드, 시작 화면·대시보드 카드에 짧은 순차 등장 효과 추가.
- 로그인·확인 대화상자·미리보기의 등장 효과와 버튼·메뉴·독·파일 항목의 상호작용 전환 통일.
- 창 크기 조절 시 가로·세로 전환 지연을 제거하고 최소화 효과 유지. Pulse 원본 이미지의 장식 없는 표시 유지.
- 모바일에서는 등장 시간 단축 및 카드 지연 제거. 동작 줄이기 설정에서는 애니메이션·전환·부드러운 스크롤 비활성화.
- 상시 반복 효과 추가 없이 CSS 중심으로 구성. 요청에 따라 실행 테스트 없이 선택자·스타일·버전 정적 검토.

---

## [v1.6.3] - 2026-09-19
### 웹 업데이트 후 재시작 연결
- Git 다운로드 성공 시 서버가 직접 재시작을 예약하도록 변경. 브라우저의 두 번째 요청 없이 재시작 진행.
- 관리 모드 여부와 파일·터미널 작업 상태를 다운로드 전에 확인. 재시작까지 작업 잠금을 유지하고 중복 재시작 요청 처리.
- 재시작 실행 실패를 서버 로그 및 상태 API에 기록하고 웹에서 복구 명령 안내.
- 이전 웹 화면의 별도 재시작 요청 및 이전 서버 응답과 호환 유지.
- 실행 테스트 없이 Python·JavaScript 구문과 재시작·잠금 해제 경로 정적 검토. 기존 서버는 이번 코드 적용 시 Termux에서 한 번 재시작 필요.

---

## [v1.6.2] - 2026-09-19
### Pulse 아이콘 원본 표시
- 시작 화면·상단 내비게이션·파일 사이드바·OS 메뉴·정보 화면의 Pulse 이미지에 추가한 둥근 모서리와 박스 그림자 제거.
- 이미지 자르기 대신 전체 원본 비율을 유지하고 시작 화면 아이콘의 확대·부유 효과 제거.
- 실행 테스트 없이 스타일과 변경 코드 정적 검토.

---

## [v1.6.1] - 2026-09-19
### 수동 업데이트 확인 수정
- 상단 새로고침에서 화면 데이터와 Git 업데이트를 함께 확인하도록 수정(관리자 전용).
- 수동 업데이트 확인은 서버의 5분 캐시를 건너뛰고 원격 저장소를 조회. 주기적 자동 확인의 캐시는 유지.
- Git 조회 실패 결과를 캐시하지 않고 다음 요청에서 재시도하도록 수정.
- 새로고침 버튼 설명 및 사용 문서 갱신. 실행 테스트 없이 구문·코드 정적 검토.

---

## [v1.6.0] - 2026-09-19
### 상단 버튼 및 CPU 표시 수정
- 메인 우측의 중복 회전 아이콘 제거. 전역 새로고침은 데이터 갱신만 수행하고 업데이트는 버전 관리 영역으로 통합.
- 기기 CPU 조회 제한·초기 샘플 부족 시 Pulse 프로세스 CPU 실측값으로 대체하고 측정 범위·수집 경로·샘플 간격 표시.
- 부하 평균을 사용률로 환산하던 경로 제거. 코어별 CPU, I/O 대기, 주파수와 프로세스 CPU를 구분.

### 운영 진단 확장
- 서버 RSS·최대 RSS·스레드·파일 핸들·CPU 시간·디스크 I/O, 메모리 상세·스왑 추가.
- 저장소 경로·쓰기 권한·가용/예약 공간·아이노드, 파일 유형별 개수·휴지통·로그 크기 표시.
- 인터페이스별 송수신 속도·누적 트래픽·오류·드롭, 브라우저 왕복 시간 추가.
- HTTP 요청·활성 요청·평균/P95 처리 시간·4xx/5xx·요청 빈도 표시. 표본은 최대 300건으로 제한하고 요청 본문은 수집하지 않음.
- 기기/서버 가동 시간, 커널·아키텍처, 온도 센서, 작업 상태, 인증 쿠키·재시작 설정, noVNC 로컬 포트 확인 추가.
- 읽을 수 없는 장치 지표는 원인 안내. 높은 자원 사용·서버 오류·저장소 권한 경고와 조회 실패 시 이전 데이터 표시.

### 추이와 조회 제어
- CPU·RSS 최대 60개 관측 그래프, 진단 JSON 내보내기, 5/15/30/60초 갱신과 일시 정지 추가.
- 지표 5초·느린 센서/보관함 정보 60초 캐시, 휴지통 탐색 최대 5,000개 항목 제한.
- 중복 대시보드 요청 병합 및 요청 시간 제한. 비활성 탭·가려진 모바일 모니터의 주기 조회 중단.
- 추가 패키지 없이 Python 표준 라이브러리로 수집. Python·JavaScript 구문 및 DOM 참조 정적 검토. 요청에 따라 실행 테스트와 실기기 검증은 수행하지 않음.

---

## [v1.5.2] - 2026-09-19
### 전역 단축 명령어 개선
- `install-cli.sh`로 기존 설치에서도 의존성 재설치 없이 `termux-cloud` 등록·갱신 지원.
- 계정 등록(`account`/`setup`), 재시작, 로그 조회, 경로 확인, 재등록, 도움말 추가.
- 인자가 없는 `restart`·`update`는 기본 백그라운드 실행. 기존 `--bg`·`status`·`stop` 명령 유지.
- 최초 시작 시 계정 등록 안내, 알 수 없는 명령은 서버를 시작하지 않고 오류 안내.
- Termux의 실제 Bash 경로 사용 및 프로젝트 경로 인용 처리. 전역 실행 파일은 저장소의 `cli.sh`를 호출해 Git 업데이트 반영.
- 설치기 등록 로직을 공통 스크립트로 통합하고 CLI 업데이트 시 전역 실행 파일도 갱신. 등록·사용 문서 추가.
- 실행 테스트 없이 Bash·Python 구문 및 코드 검토. 스마트폰의 실제 전역 등록은 기기에서 실행 필요.

---

## [v1.5.1] - 2026-09-19
### 서버 종료 회귀 수정
- 상대 경로 `python3 app.py` 실행도 작업 폴더를 확인해 식별하도록 수정. Termux/Linux의 `/proc`과 macOS의 `ps`·`lsof` 지원.
- PID 파일이 없거나 오래된 경우 프로젝트 서버를 다시 찾고 Flask 리로더 자식 프로세스도 함께 종료.
- 종료 요청 후 남은 서버를 확인하고 실패 시 오류와 PID 파일을 보존. 다른 프로젝트 프로세스는 종료하지 않음.
- 시작·상태 조회에도 같은 식별 로직을 사용하고, 업데이트 중 종료 실패 시 재시작 중단.
- Python·Bash 구문과 코드 경로 검토. 현재 macOS 작업 환경의 서버와 리로더 종료 확인. 자동화 테스트 및 Termux 실기기 테스트는 수행하지 않음.

---

## [v1.5.0] - 2026-09-19
### 로그인과 운영 안전성
- Termux에서 관리자·조회 계정을 등록하는 `setup-auth.py` 추가. 비밀번호 해시, 서명된 12시간 세션, CSRF 검증, 로그인 시도 제한 적용.
- 계정 미등록 시 파일·관리 API 잠금. 관리자만 파일 변경·명령 실행·업데이트 가능.
- 디버그 기본값 비활성화. Node.js 진입점도 동일 Python 백엔드를 사용하도록 통합.
- 파일 경로·심볼릭 링크 검증, 파일 URL 인코딩, 미리보기 격리, 릴리즈 노트 HTML 이스케이프 적용.
- 프로젝트 경로와 PID를 확인해 서버를 종료하고, 다른 프로젝트를 대상으로 한 광범위 프로세스 종료 제거.

### Pulse OS와 모바일 사용성
- 터미널·앱 창·독·설정의 이전 브랜딩 정리 및 실제 버전·기기 정보 표시.
- 모바일에서 선택한 앱 하나만 표시하고 화면 높이·키보드 변화에 대응. 독 중심 탐색과 실행 앱 강조.
- 첫 진입 시 터미널 자동 실행 제거. 앱 아이콘을 공통 SVG 스타일로 통일.
- 터미널 실행 상태, 서버·클라이언트 중복 실행 방지, 15초 제한, 출력 1MB 제한 및 자식 프로세스 종료 처리.
- 에디터의 미저장 변경 보호, 저장 중 입력 잠금, 불러오기 실패 시 기존 내용 유지, 저장 재시도 제공.
- 공통 입력·확인 대화상자와 재시도 알림 적용.

### 파일 관리와 스마트폰 자원 사용
- 폴더 생성·탐색, 파일·폴더 이름 변경과 이동, 페이지 다중 선택 추가.
- 삭제 항목을 휴지통에 보관하고 개별 복원·영구 삭제 지원. 충돌 시 덮어쓰지 않음.
- 서버에서 현재 폴더 검색·정렬·페이지 분할(기본 60개). 업로드 동시 전송을 2개로 제한.
- Pillow 설치 시 이미지 썸네일 생성 및 최대 200개 캐시. 미설치 환경은 파일 아이콘으로 대체.
- 텍스트 편집·미리보기 2MB 제한. 바이너리 문서는 다운로드로 안내.
- CPU 실측·추정·측정 불가 구분, 고정 메모리·배터리 정상값 제거.
- 숨긴 탭의 주기 조회 중단, 활성 모니터 15초 갱신, 서버 측 지표·파일 통계 캐시.

### 업데이트와 Linux 연결
- 웹 업데이트를 다운로드·재시작·재접속 확인 단계로 구분. 관리 실행 환경이 아니면 수동 재시작 안내.
- Git fast-forward만 허용하고 미커밋 변경 시 중단. 업데이트 확인 결과는 서버에서 5분 재사용.
- noVNC 포트와 접속 주소 설정, HTTPS/HTTP 혼합 연결 안내, 새 탭 주소 일치 처리.
- 최초 계정 설정·권한·선택 썸네일 의존성·기존 설치 업그레이드 절차 문서화.

### 검토 범위
- 요청에 따라 실행 테스트는 생략하고 Python AST, JavaScript 구문, Bash 구문 및 코드 경로를 정적으로 검토.
- 실제 Termux 기기, 모바일 키보드, noVNC 및 서버 재시작 동작의 실행 검증은 수행하지 않음.

---

## [v1.4.0] - 2026-09-19
### ⚡ 브랜딩 & 디자인 전면 개편 (Rebranding & Identity)
- **공식 서비스 브랜드명 'Pulse' 개편**:
  - 마스터 브랜드 **Pulse** 아래 **Pulse Cloud**(개인 클라우드 드라이브)와 **Pulse OS**(가상 컴퓨터 Web OS) 2대 핵심 서비스 체제로 공식 분리 및 운영.
- **전용 3D 펄스(Pulse) 아이콘 도입**:
  - 전용 3D Squircle 펄스 아이콘을 브라우저 파비콘, 홈 화면 바로가기, 글로벌 내비게이션, 포털 히어로 엠블럼, Pulse OS About 로고에 전면 적용.
- **UI/UX 개선 및 파일 삭제 정상화**:
  - 파일 카드 그리드 및 리스트 뷰에서 긴 파일명 말줄임 처리 및 다운로드/삭제 플로팅 액션 버튼 레이아웃 최적화 (모바일/데스크탑 반응형 분리).
  - Apple 스타일 블러 삭제 확인 모달 정상화 및 삭제 시 보관함/Finder/스토리지/대시보드 실시간 동기화.

---

## [v1.3.0] - 2026-09-19
### ✨ 신규 기능 (New Features)
- **메인 시작 포털 [컴퓨터 사용 (가상 데스크탑)] 신설**:
  - 시작 화면에 [iCloud 드라이브], [컴퓨터 사용 (가상 데스크탑)], [서버 대시보드] 3대 핵심 런처 카드 구성.
  - 상단 내비게이션 바에 **[🖥️ 가상 데스크탑]** 탭 추가로 원클릭 즉시 전환.
- **웹 브라우저 속 가상 데스크탑 GUI (Cloud Web OS)**:
  - macOS 스타일의 상단 글로벌 메뉴바(Apple 정보, 활성 앱 이름, 배터리/IP/디지털 시계/전체화면 토글).
  - 다이나믹 바탕화면 테마 4종(Sonoma, Sequoia, Cyber Neon, Midnight Ocean) 및 하단 플로팅 독(Dock).
  - **멀티 윈도우 창 관리 시스템**: 마우스 & 스마트폰 터치 드래그 이동, 우하단 리사이즈, 신호등(닫기/최소화/최대화), 창 포커스(z-index).
- **가상 컴퓨터 내장 앱 세트**:
  - 💻 **대화형 터미널 (Interactive Terminal)**: 스마트폰 호스트의 실제 쉘 명령어 실행(`POST /api/terminal/exec`), 작업 디렉토리(`cd`) 탐색, 명령어 히스토리(방향키), 자주 쓰는 퀵 버튼 탑재.
  - 📁 **내 보관함 탐색기 (Finder)**: 클라우드 보관 파일 목록 열람, 파일 더블클릭 시 코드 에디터 연동, `+ 새 파일` 즉시 생성.
  - 📝 **코드 & 텍스트 에디터 (Code Studio)**: 라인 넘버 표시, 실시간 라인/글자 수 카운트, 파일 수정 후 디스크에 즉시 저장(`POST /api/files/save`, 단축키 `Ctrl+S`).
  - 📊 **활동 모니터 (Activity Monitor)**: 실시간 CPU, RAM, 배터리, 디스크 사용량 게이지 및 서버 정보 플로팅 위젯.
  - 🌐 **웹 브라우저 (Browser)**: 주소창 및 탐색이 가능한 미니 웹 브라우저 창.
  - 🐧 **리눅스 GUI (X11 / noVNC) 연동기**: 스마트폰에 완전한 XFCE4 데스크탑을 구축하는 원클릭 스크립트(`setup-desktop.sh`) 및 포트 6080 임베디드 뷰어 연결 지원.
  - ⚙️ **환경설정 & 이 컴퓨터에 관하여**: 배경화면 변경 및 기기 하드웨어 사양 요약.

---

## [v1.2.0] - 2026-09-19
### ✨ 신규 기능 (New Features)
- **메인 시작 포털 화면**: 웹 접속 시 [☁️ iCloud 드라이브]와 [⚡ 서버 상태 대시보드]를 선택하여 진입할 수 있는 포털 런처 신설.
- **스마트폰 서버 종합 상태 대시보드**:
  - 실시간 CPU, 메모리(RAM), 디스크 저장공간 분석 게이지.
  - 안드로이드 스마트폰 배터리 상태(잔량 %, 충전 여부, 온도) 모니터링 연동.
  - 실시간 서버 Uptime(가동 시간), 프로세스 PID, 로컬 Wi-Fi IP 안내.
  - 버전 정보 및 이번 업데이트 상세 변경 내역 뷰어 탑재.
- **무다운로드 파일 미리보기(Preview) 전면 지원**:
  - 사진/동영상/오디오/PDF뿐만 아니라 **txt, md, json, py, js, log, csv, yaml, html, css** 등 텍스트 및 코드 파일 다운로드 없이 브라우저에서 즉시 열람 지원.
  - 라인 번호 표시 및 **[전체 내용 복사]** 편의 기능 제공.
- **버전 관리 및 변경점 알림 시스템**:
  - 원격 업데이트 감지 시 "이번 업데이트에서 무엇이 변경되었는지" 커밋 메시지와 릴리즈 노트를 팝업으로 상세 확인 가능.

---

## [v1.1.0] - 2026-09-19
### ✨ 신규 기능 (New Features)
- **실시간 코드 업데이트 자동 감지 배너**:
  - PC에서 `git push` 실행 시 30초 이내에 모바일 화면 상단에 Dynamic 알림 배너 슬라이드 등장.
  - 사용자가 원할 때 **[지금 업데이트 적용]** 버튼을 눌러 원클릭으로 최신 코드 자동 pull 및 브라우저 새로고침.
- **로컬 Wi-Fi IP 자동 감지 대시보드**:
  - 서버 시작 시 같은 와이파이 내 다른 기기에서 접속할 수 있는 스마트폰 실제 IP(`192.168.X.X`) 자동 화면 출력.
- **터미널 단축 명령어 및 백그라운드 구동**:
  - `termux-cloud --bg` 24시간 백그라운드 데몬 구동 지원.
  - `status.sh`, `stop.sh`, `update.sh` 생명주기 관리 스크립트 완비.

---

## [v1.0.0] - 2026-09-19
### 🚀 초기 릴리즈 (Initial Release)
- **iCloud 스타일 UI**: 애플 글래스모피즘, 반응형 그리드/리스트 뷰, 카테고리별 탐색.
- **미디어 라이트박스 뷰어**: 사진/동영상 풀스크린 뷰어 및 키보드 좌우 탐색.
- **드래그 앤 드롭 업로드 & 다운로드/삭제**: 다중 파일 업로드 및 실시간 진행률.
- **Termux 지원**: 안드로이드 Termux 환경에서 Python Flask 백엔드 구동 지원.
